## 方針

- 既存のコードをできるだけ変更せずに機能追加を行ってください．
  ただし，既存のコードに不備があったり，リファクタリングすることによってより良いソースコードになったりする場合は，積極的に提案してください．
- 重複したロジックを生成せずに，できるだけ統合するように心掛けてください．
- ユーザに提示するメッセージ（console，Error 等）は英語で記述してください．
- テスト用コードを本体に加えないでください．

## ツールキット

- npm ではなく yarn を使用してください．

## コーディングスタイル

- トップレベルに関数を定義する場合，一行で `() => returnValue` のように書かずに，return 文を用いて複数行で以下のように書いてください．
- if 文や for 文の内容が 1 行の場合でも，中括弧を省略しないでください．
- 変数名は慣習的なもの（例：ループ変数の i, j, k）を除いて，一文字で表記しないでください．
- `yarn run check` でコードフォーマッタを実行できます．修正すべきエラーがないことを確認してください．

### コメント

- ソースコード中のコメントおよびドキュメントの句読点はカンマ（，），ピリオド（．）を使用してください．
- コメント内で見出しを作りたい場合，以下のように記述してください．
  ```ts
  // ------
  // 見出し
  // ------
  ```
- TSDoc
  - TSDoc を書く場合は，複数行で書くようにしてください（ただしフィールドを除く）．
  - TSDoc の関数の定義は「〜する」と動詞で書いてください．それ以外の箇所では，可能な限り体言止めで書いてください．
  - デフォルト値を記述する場合は @default を使用してください．
